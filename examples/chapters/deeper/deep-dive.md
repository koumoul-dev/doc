#### Deep dive (from `chapters/deeper/deep-dive.md`)

This file sits two directories below the root document. The icon below uses `./icon.svg` and resolves relative to this file's directory — proving that nested includes keep their own path context.

![An icon that lives in chapters/deeper/](./icon.svg)
